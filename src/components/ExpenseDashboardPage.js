import React from 'react';

// Stateless Functional Component using arrow function with implicit return.  
const ExpenseDashboardPage = () => (
   <div>
      This is from the Dashboard Component
   </div>
);
// This valid React component can now be passed into the component prop (in the Route component below) as it's value.

export default ExpenseDashboardPage;